import React from 'react';

const Header: React.FC = () => (
  <header className="fixed top-0 left-0 right-0 bg-blue-600 text-white p-4 text-center font-bold text-xl z-10">
    AI Toolbox
  </header>
);

export default Header;